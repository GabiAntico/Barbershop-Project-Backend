spring.application.name=shifts
spring.profiles.active=${SPRING_PROFILES_ACTIVE:dev}

app.jwt.secret=${APP_JWT_SECRET:una-clave-larga-de-minimo-32-caracteres-cambiala}
app.frontend.url=${APP_FRONTEND_URL:http://localhost:4200}
